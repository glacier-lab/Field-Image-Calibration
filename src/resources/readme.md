Step 1: Unzip iCalibrateImagesInstaller.zip
Step 2: In the terminal run as admin and run:

sudo xattr -rd com.apple.quarantine iCalibrateImagesInstaller.app

You may need to change the path to the app. 
Step 3: Then simply open the installer and follow the instructions.